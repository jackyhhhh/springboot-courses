spring:
  profiles:
    active: dev # 默认选择dev环境
#    active: pro # 默认选择pro环境

---
spring:
  profiles: dev
server:
  port: 8888  # 指定要监听的接口
  servlet:
    context-path: /dev # 指定上下文路径

---
spring:
  profiles: pro
server:
  port: 9999  # 指定要监听的接口
  servlet:
    context-path: /pro # 指定上下文路径